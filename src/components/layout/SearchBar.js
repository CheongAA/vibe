import React from 'react';

const SearchBar = () => {
  return (
    <input
      className="mt-5 w-[177px] h-[40px] bg-gray-800 rounded-sm p-[10px] font-bold text-white"
      placeholder="VIBE 검색"
    />
  );
};

export default SearchBar;
